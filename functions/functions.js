//This is a code snippet showcasing a function
function myAnimal (animal) {
    return animal + " is my favorite animal";
}

console.log(myAnimal("Goat"));

//second function to add a number to 7
function add7 (number) {
    return number + 7;
}

console.log(add7(7));

//third function that multiplies two numbers 
function multiply (number1, number2) {
    return number1 * number2;
}

console.log(multiply(10, 10));

//function to return last letter of string
function lastLetter (Rodgers) {
    return 
}


//function to capitalize the first letter of a string
function capitalize (string) {
    return 
}

//THOSE ABOVE HERE WERE USER DEFINED FUNCTIONS HERE ARE INBUILT BROWSER FUNCTIONS

const myNumber = Math.random();//math.random() generates random numbers that are decimals less than 1
console.log(myNumber);

const myArray = ["Rodgers", "Fanuel", "Mwangi"];
console.log(myArray.join());//<arrayName>.join() joins the items of an array into a single string

const myString = "I am a string";
const stringReplacement = myString.replace("string", "STRING");//<stringName>.replace() replaces a string with another
console.log(stringReplacement);