console.log((3 + 2) - 76 * (1 + 1));

//the Number() constructor

let myNumber = "74";
//re assign using the Number() constructor

myNumber = Number(myNumber) + 3;
console.log(myNumber);

//a ternary operator 
const birthday  = true;
let neighbor = birthday
 ? "that is true" 
 :"that is false";